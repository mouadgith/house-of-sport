class Coach {
  constructor(id, nom, prenom, tel, adresse, sexe, dateNaissance) {
    this.id = id;
    this.nom = nom;
    this.prenom = prenom;
    this.tel = tel;
    this.adresse = adresse;
    this.sexe = sexe;
    this.dateNaissance = dateNaissance;
  }
}

export default Coach; 